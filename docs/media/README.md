# Screenshots

The three images the top-level README embeds. Drop them here under exactly
these names:

| File | What it should show |
|---|---|
| `agent.png` | The app open on an agent — transcript, and ideally a selection prompt with the key palette below it. |
| `projects.png` | The project list, so the flock and the recency order are visible. |
| `menubar.png` | The macOS menu bar app, dropped open. |

Phone shots straight from the iPhone are the right size; nothing needs
resizing. Keep them under a megabyte or so each — GitHub serves them on every
visit to the front page.
